import { MatStepperScrollerDirective } from './mat-stepper-scroller.directive';

describe('MatStepperScrollerDirective', () => {
  it('should create an instance', () => {
    const directive = new MatStepperScrollerDirective();
    expect(directive).toBeTruthy();
  });
});
